.row.col-md-10.col-md-offset-1
  %i.fa.fa-clock-o{"aria-hidden" =>"true"}
  %span
  Posted on #{@tag.created_at.strftime("%b %d, %Y")}
  = @tag.name

  .jumbotron.col-md-10.col-md-offset-1.text-info
    .row
      .col-md-12
        %h2 Emails
        .row.col-md-12{:style => "margin: 10px 0;border: 1px solid #e3e3e3;padding: 5px"}
          
          - @tag.mailers.each do |mail|
            = mail.name  | 
            = mail.email | 
            = mail.mobile
            %br/